diff -aur weston-3.0.0.orig/libweston/vaapi-recorder.c weston-3.0.0.new/libweston/vaapi-recorder.c
--- weston-3.0.0.orig/libweston/vaapi-recorder.c	2017-08-08 14:57:03.000000000 -0400
+++ weston-3.0.0.new/libweston/vaapi-recorder.c	2018-05-05 03:03:18.291133873 -0400
@@ -36,6 +36,7 @@
 #include <sys/types.h>
 #include <sys/stat.h>
 #include <fcntl.h>
+#include <signal.h>
 
 #include <pthread.h>
 
@@ -900,10 +901,20 @@
 static int
 setup_worker_thread(struct vaapi_recorder *r)
 {
+	sigset_t sig_parentset;
+	sigset_t sig_childset;
+
+
+	sigemptyset( &sig_childset );
+	sigaddset( &sig_childset, SIGUSR1 );
+	pthread_sigmask( SIG_BLOCK, &sig_childset, &sig_parentset );
+
 	pthread_mutex_init(&r->mutex, NULL);
 	pthread_cond_init(&r->input_cond, NULL);
 	pthread_create(&r->worker_thread, NULL, worker_thread_function, r);
 
+	pthread_sigmask( SIG_SETMASK, &sig_parentset, NULL );
+
 	return 1;
 }
 
